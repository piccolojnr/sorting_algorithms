#include "sort.h"

void counting_sort(int *array, size_t size)
{
    int max = array[0];
    int min = array[0];
    int i, j;
    size_t k;
    int *count = NULL;
    int *sorted = NULL;
    int *temp = NULL;
    
}